See how mask detection varies with image size --- which needs to be an advanced variable.

Create in Settings Window:
    Video FileName

    Timestamp for subject text in email tab

    Change emergency stop to regular stop -that can save images.
    Two pressed means emergency stop.


(optional)    Display image in mainwindow option.
(optionnal)    Display image in mainwindow with overlay of subtracted image for showing where motion occured.
